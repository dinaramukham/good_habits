import os
import requests

#from good_habits.good_habits.good_habits.settings import TELEGRAM_TOKEN


#from ..settings import TELEGRAM_TOKEN


class MyBot():
    URL = 'https://api.telegram.org/bot'
    #TOKEN = TELEGRAM_TOKEN

    #def send_message(self, text):
    #    requests.post(
    #        url=f'{self.URL}{self.TOKEN}/sendMessage',
    #        data={
    #            'chat_id': '742481601',
    #            'text': text,
    #        }
    #    )
